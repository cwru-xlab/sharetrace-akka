package org.sharetrace.message;

import org.sharetrace.graph.ContactGraph;
import org.sharetrace.graph.Node;

/** A marker for all messages that can be sent by a {@link Node} in a {@link ContactGraph}. */
public interface NodeMessage {}
